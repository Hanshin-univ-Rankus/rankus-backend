# .github/workflows/ci-dev.yml
name: CI Dev Build & Deploy

on:
  push:
    branches: [ dev ]

env:
  AWS_REGION: ap-northeast-2
  S3_BUCKET: ${{ secrets.S3_BUCKET }}

jobs:
  build:
    name: Build & Upload to S3
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up JDK 17
        uses: actions/setup-java@v3
        with:
          distribution: temurin
          java-version: '17'

      - name: Build application JAR
        run: |
          cd rankus
          chmod +x ./gradlew
          ./gradlew clean bootJar

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v2
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Upload JAR to S3
        run: |
          aws s3 cp rankus/build/libs/app.jar s3://${{ env.S3_BUCKET }}/app.jar

      - name: Verify S3 upload
        run: |
          aws s3 ls s3://${{ env.S3_BUCKET }}/app.jar

  deploy:
    name: Download & Deploy to EC2
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Configure AWS credentials (for S3 download)
        uses: aws-actions/configure-aws-credentials@v2
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Download JAR from S3
        run: |
          aws s3 cp s3://${{ env.S3_BUCKET }}/app.jar ./app.jar

      - name: Verify downloaded JAR
        run: |
          ls -lh app.jar

      - name: Install SSH key
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.EC2_SSH_KEY }}" > ~/.ssh/id_rsa
          chmod 600 ~/.ssh/id_rsa

      - name: Copy JAR to EC2 (as temp file)
        run: |
          scp -o StrictHostKeyChecking=no \
            -i ~/.ssh/id_rsa \
            ./app.jar \
            ${{ secrets.EC2_SSH_USER }}@${{ secrets.EC2_HOST }}:/home/ec2-user/app/app-new.jar

      - name: SSH into EC2 & Restart Service
        run: |
          ssh -t -o StrictHostKeyChecking=no \
            -i ~/.ssh/id_rsa \
            ${{ secrets.EC2_SSH_USER }}@${{ secrets.EC2_HOST }} << 'EOF'
          set -e
          cd /home/ec2-user/app
          
          # 기존 프로세스 종료 (에러가 발생해도 계속 진행)
          pkill -f 'java -jar app.jar' || true
          
          # 이전 JAR 백업 (있을 경우)
          if [ -f app.jar ]; then
            mv app.jar app-backup-$(date +'%Y%m%d%H%M%S').jar
          fi
          
          # 새 JAR 파일로 교체
          mv app-new.jar app.jar
          
          # 환경변수 설정
          export SPRING_DATASOURCE_URL='${{ secrets.SPRING_DATASOURCE_URL }}'
          export SPRING_DATASOURCE_USERNAME='${{ secrets.SPRING_DATASOURCE_USERNAME }}'
          export SPRING_DATASOURCE_PASSWORD='${{ secrets.SPRING_DATASOURCE_PASSWORD }}'
          export JWT_SECRET='${{ secrets.JWT_SECRET }}'
          
          # 새 JAR 실행
          nohup java -jar app.jar --spring.profiles.active=dev > deploy.log 2>&1 &
          
          echo "애플리케이션 재시작 완료"
          sleep 5
          ps -ef | grep java
EOF
